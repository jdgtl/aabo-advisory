---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<div class="py-14 lg:py-20">
  <div class="relative max-w-[720px]">
    <span
      aria-hidden="true"
      role="presentation"
      class="pointer-events-none select-none absolute -top-8 lg:-top-12 left-0 font-heading font-bold leading-none text-primary/[0.07] z-0"
      style="font-size: clamp(80px, 11vw, 130px);"
    >
      &ldquo;
    </span>
    <blockquote
      class="relative z-[1] pl-8 sm:pl-10 font-heading italic text-[16px] sm:text-[18px] lg:text-[20px] leading-[1.6] text-primary"
    >
      {text}
    </blockquote>
  </div>
</div>
